import Button from "@mui/material/Button";
import * as React from "react";

const Home = () => {
  return (
    <div>
      This is Login page
      <Button variant="text">Stepper</Button>
    </div>
  );
};

export default Home;
